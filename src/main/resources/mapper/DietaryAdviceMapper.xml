<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lc.sunnyside.mapper.DietaryAdviceMapper">

    <select id="selectByPatientIdAndDate" resultType="cn.lc.sunnyside.POJO.DO.DietaryAdvice">
        SELECT *
        FROM dietary_advice
        WHERE patient_id = #{patientId}
          AND meal_date = #{mealDate}
        ORDER BY meal_type
    </select>

    <select id="selectByPatientIdAndDateAndType" resultType="cn.lc.sunnyside.POJO.DO.DietaryAdvice">
        SELECT *
        FROM dietary_advice
        WHERE patient_id = #{patientId}
          AND meal_date = #{mealDate}
          AND meal_type = #{mealType}
    </select>

</mapper>
