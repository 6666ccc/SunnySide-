<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lc.sunnyside.mapper.HealthRecordMapper">

    <select id="selectByElderIdAndDateRange" resultType="cn.lc.sunnyside.POJO.DO.HealthRecord">
        SELECT *
        FROM health_record
        WHERE elder_id = #{elderId}
          AND record_date &gt;= #{startDate}
          AND record_date &lt;= #{endDate}
        ORDER BY record_date DESC, record_time DESC
    </select>

    <select id="selectLatestByElderIdLimit" resultType="cn.lc.sunnyside.POJO.DO.HealthRecord">
        SELECT *
        FROM health_record
        WHERE elder_id = #{elderId}
        ORDER BY record_date DESC, record_time DESC
        LIMIT #{limit}
    </select>

</mapper>
