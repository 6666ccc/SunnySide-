# SunnySide SQL 对齐待办清单

> 规则：以 `sql.sql` 为唯一基线；`todo.md` 中不受 SQL 支撑的需求已标注“舍弃（待设计）”。

## P0（本轮直接落地）

- [ ] **补齐 `family_user` 数据访问层**
  - 交付：DO、Mapper、XML、基础查询能力。
- [ ] **补齐 `family_elder_relation` 数据访问层**
  - 交付：DO、Mapper、XML、按家属/老人关系查询与校验能力。
- [ ] **补齐 `health_record` 数据访问层**
  - 交付：DO、Mapper、XML、按老人+日期查询能力。
- [ ] **实现家属绑定关系校验服务**
  - 交付：基于 `family_elder_relation` 的权限校验接口。
- [ ] **实现家属健康查询与老人日汇总服务**
  - 交付：健康记录摘要 + 活动/菜单/公告/探访的当日聚合查询。
- [ ] **扩展 AI Tool 家属查询能力**
  - 交付：`queryElderHealth`、`getElderDailySummary`（仅 SQL 可支持子集）。

## P1（保留并按 SQL 子集执行）

- [ ] **活动报名冲突检测（保留）**
  - 说明：可基于 `activity_participation` + `activity_schedule` 实现。
  - 舍弃子项：候补队列、排队序号、自动递补（缺表/缺字段）。
- [ ] **来访预约审批流（部分保留）**
  - 说明：保留 `APPROVED` 路径与进度查询。
  - 舍弃子项：`REJECTED` 与审批备注（`visit_appointment` 当前无对应枚举/字段）。
- [ ] **家属端探访预约（保留）**
  - 说明：可复用 `visit_appointment` 完成预约与状态查询。
  - 舍弃子项：留言推送（缺留言表与消息分发模型）。
- [ ] **公告优先级提示（部分保留）**
  - 说明：可利用 `announcement.priority` 输出高优先级摘要。
  - 舍弃子项：按人群标签过滤、过期自动降权（缺标签与过期字段）。

## 已舍弃功能（待你确认是否补充数据库设计）

- [ ] **活动候补机制**
  - 原因：缺少候补记录表、候补状态、顺位字段。
- [ ] **预约拒绝闭环**
  - 原因：缺少 `REJECTED` 状态与审批备注字段。
- [ ] **个性化健康规则引擎**
  - 原因：缺少老人标签模型（慢病/作息/饮食禁忌）。
- [ ] **留言互动与消息通知**
  - 原因：缺少留言表、消息表、投递状态模型。
- [ ] **吃药提醒能力**
  - 原因：缺少 `medication_plan` 表及服药执行记录模型。
- [ ] **SOS 紧急联络**
  - 原因：缺少 SOS 事件、通知渠道、回执状态模型。
- [ ] **公告人群分发与自动过期**
  - 原因：缺少人群标签与过期时间字段。

## 文案纠正（与 SQL 冲突项）

- [ ] `family_user`、`family_elder_relation`、`health_record` 已在 `sql.sql` 中存在，不再描述为“新增表”。
